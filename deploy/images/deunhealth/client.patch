--- a/cmd/app/main.go
+++ b/cmd/app/main.go
@@ -108,2 +107,0 @@
-	docker.NegotiateVersion(ctx)
-
--- a/internal/docker/docker.go
+++ b/internal/docker/docker.go
@@ -12 +12 @@
-	client, err := client.NewClientWithOpts(client.WithHost(dockerHost), client.WithAPIVersionNegotiation())
+	client, err := client.New(client.WithHost(dockerHost))
--- a/internal/docker/labeled.go
+++ b/internal/docker/labeled.go
@@ -7,3 +7 @@
-	"github.com/docker/docker/api/types/container"
-	"github.com/docker/docker/api/types/events"
-	"github.com/docker/docker/api/types/filters"
+	"github.com/moby/moby/client"
@@ -16 +14 @@
-	filtersArgs := filters.NewArgs()
+	filtersArgs := make(client.Filters)
@@ -21 +19 @@
-	list, err := d.client.ContainerList(ctx, container.ListOptions{
+	list, err := d.client.ContainerList(ctx, client.ContainerListOptions{
@@ -28,2 +26,2 @@
-	containers = make([]Container, len(list))
-	for i, container := range list {
+	containers = make([]Container, len(list.Items))
+	for i, container := range list.Items {
@@ -46 +44 @@
-	filtersArgs := filters.NewArgs()
+	filtersArgs := make(client.Filters)
@@ -52 +50 @@
-	messages, errors := d.client.Events(ctx, events.ListOptions{
+	stream := d.client.Events(ctx, client.EventsListOptions{
@@ -54,0 +53 @@
+	messages, errors := stream.Messages, stream.Err
--- a/internal/docker/unhealthy.go
+++ b/internal/docker/unhealthy.go
@@ -6,3 +6 @@
-	"github.com/docker/docker/api/types/container"
-	"github.com/docker/docker/api/types/events"
-	"github.com/docker/docker/api/types/filters"
+	"github.com/moby/moby/client"
@@ -13 +11 @@
-	filtersArgs := filters.NewArgs()
+	filtersArgs := make(client.Filters)
@@ -17 +15 @@
-	containers, err := d.client.ContainerList(ctx, container.ListOptions{
+	containers, err := d.client.ContainerList(ctx, client.ContainerListOptions{
@@ -24 +22 @@
-	unhealthies = make([]Container, len(containers))
+	unhealthies = make([]Container, len(containers.Items))
@@ -26 +24 @@
-	for i, container := range containers {
+	for i, container := range containers.Items {
@@ -41 +39 @@
-	filtersArgs := filters.NewArgs()
+	filtersArgs := make(client.Filters)
@@ -46 +44 @@
-	messages, errors := d.client.Events(ctx, events.ListOptions{
+	stream := d.client.Events(ctx, client.EventsListOptions{
@@ -48,0 +47 @@
+	messages, errors := stream.Messages, stream.Err
--- a/internal/docker/name.go
+++ b/internal/docker/name.go
@@ -6,2 +6,2 @@
-	"github.com/docker/docker/api/types/container"
-	"github.com/docker/docker/api/types/events"
+	"github.com/moby/moby/api/types/container"
+	"github.com/moby/moby/api/types/events"
--- a/internal/docker/message.go
+++ b/internal/docker/message.go
@@ -3 +3 @@
-import "github.com/docker/docker/api/types/events"
+import "github.com/moby/moby/api/types/events"
--- a/internal/docker/restart.go
+++ b/internal/docker/restart.go
@@ -6 +6 @@
-	"github.com/docker/docker/api/types/container"
+	"github.com/moby/moby/client"
@@ -10 +10,2 @@
-	return d.client.ContainerRestart(ctx, name, container.StopOptions{})
+	_, err = d.client.ContainerRestart(ctx, name, client.ContainerRestartOptions{})
+	return err
--- a/internal/docker/version.go
+++ /dev/null
@@ -1,7 +0,0 @@
-package docker
-
-import "context"
-
-func (d *Docker) NegotiateVersion(ctx context.Context) {
-	d.client.NegotiateAPIVersion(ctx)
-}
