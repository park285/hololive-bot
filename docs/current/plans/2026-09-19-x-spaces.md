# X 스페이스 무료 감지와 시작 알림

**Decisions:** `DEC-20260919-hololive-x-spaces` (governing), `DEC-20260919-x-spaces-admin-reconnect` (governing), `DEC-20260920-x-spaces-linux-recovery` (governing)

## Execution capsule

**Goal:** 로그인 쿠키 기반 X 내부 API로 멤버가 직접 연 스페이스를 발견하고 방송 구독 방에 시작 링크를 보냅니다.
**Context:** 기존 alarm-worker와 PostgreSQL dispatch ledger를 사용합니다. 작업 트리는 /home/kapu/work/hololive-xspaces-20260919/hololive-bot 입니다.
**Constraints:** 유료 API·외부 유료 서비스 금지. 실계정 세션 연결과 발송 설정까지 사용자 승인 범위입니다. 설정 전 기본 비활성화, 시작 알림만, 게스트 참여·녹음 제외. 기존 Windows 인증은 새 Linux 전용 계정 검증 전까지 보존합니다. 인증 값을 대화·로그에 노출하지 않고 추가 인증은 우회하지 않습니다.
**Evidence:** twspace-crawler b03071c6의 avatar_content 및 AudioSpaceById 구현, 기존 domain/alarm과 dispatchoutbox 소유 경로를 확인했습니다.
**Success:** 계정→스페이스 발견, 호스트 확인, 방별 지속 중복 방지, 오류/미상 보존, 쿠키 비노출이 모의 응답과 기존 발송 경로 검사로 검증됩니다.
**Output:** 무료 수집 helper, worker 연결, 전용 source payload와 텍스트 렌더링, 테스트, 실계정 세션·발송 설정, 사용 문서와 검증 기록.

## Implementation

### T01 수집 경로 구현

alarm-worker/xspaces 소유 Node helper에서 사용자 ID 목록과 cookie를 stdin으로 받습니다. X 웹 내부 경로만 허용하며 조회 timeout·출력 크기·redirect를 제한합니다. twspace-crawler의 전체 녹음/알림 런타임 대신 그 요청 생성에 쓰이는 x-client-transaction-id 0.3.1의 공개 API를 재사용합니다. 공식 API 자격 증명은 받지 않습니다. 일반 API 장애를 빈 목록으로 변환하지 않습니다. AC01/V01과 연결됩니다.

### T02 기존 발송 경로 연결

T01 이후 shared domain에 x_space source, worker에 poll runner를 추가합니다. 명시적 X 사용자 ID↔YouTube 채널 ID 매핑을 사용하고 기존 LIVE 구독자를 조회합니다. 스페이스 ID로 이벤트, 방 ID로 delivery를 식별합니다. title 변경/재시작에도 첫 관측 payload를 보존하는 원장을 소유하여 충돌을 피합니다. 개설자만 대상이며 새 플랫폼을 YouTube 방송으로 위장하지 않습니다. AC02/V02와 연결됩니다.

### T03 런타임과 문서 검증

T02 이후 기본 비활성화 설정, worker 수명주기, helper 실행 파일 배치와 사용 문서를 연결합니다. cookie는 DB에 인증 암호화로 보관하며 키 파일은 기존 static secret 절차를 따릅니다. 값·응답 원문은 로그에 넣지 않습니다. 수집 실패는 오류로 관측하고 발송 재시도/미상 계약은 기존 ledger를 따릅니다. AC03/V03과 연결됩니다.

### T04 실계정 세션과 발송 설정

T03 이후 사용자 윈도우 Edge의 지정 세션을 비밀 관리 절차로 연결하고 사용자 지정 검증 방에 발송을 검증합니다. 봇의 세션 만료·무효화·재인증 조건을 확인하며, 고정 쿠키 복사만으로 무인 장기 운영을 보장하지 않습니다. 지정 세션의 안전한 전달과 검증 방이 정해지기 전에는 임의 계정·방을 선택하지 않습니다. AC04/V04와 연결됩니다.

### T05 관리자 재연결 구현

T03과 병행하여 Iris Admin의 기존 Hololive 세션·권한·CSRF·mutation ID 보호 경로에 상태 조회와 세션 교체 요청을 추가합니다. 별도 작업 트리는 /home/kapu/work/hololive-xspaces-20260919/iris-admin 입니다. 새 cookie는 암호화된 후보로 저장하고 worker에서 검증 성공 시에만 기존 인증을 교체합니다. 실패한 후보는 기존 인증을 지우지 않습니다. generation 비교로 오래된 관측이 새 인증을 덮어쓰지 않게 합니다. 화면은 마지막 성공·인증 필요·후보 검증 상태와 복구 방법을 표시하며 secret 재조회와 브라우저 영속 저장은 금지합니다. T04는 T05 이후 완료합니다. AC05/V05와 연결됩니다.

### T06 스페이스 알림 양식 통일

T02 완료 후 스페이스 전용 DB 템플릿과 샘플을 등록하고 기존 렌더러의 mdsafe 처리를 재사용합니다. 방송 시작 알림과 같은 빨간 원·굵은 멤버명·제목 링크를 사용하며 제목이 없으면 URL만 표시합니다. 구독·중복 방지·전송 경로는 유지합니다. 현재 운영 API의 다른 작업 변경을 보존하고 마이그레이션과 워커만 반영합니다. AC06/V06과 연결됩니다.

### T07 Linux 로그인 복구와 지속 시도 원장

T04 이후 별도 복구 서비스가 보호된 전용 계정 설정을 읽고 Chromium/Playwright를 단발 실행합니다. 초기 전용 계정 연결 또는 확정된 auth_required만 처리하고 기존 후보 검증 경로로 쿠키를 제출합니다. 수동 제출과 충돌 시 폐기합니다. 로그인은 같은 설정·세션 세대당 한 번, 24시간당 최대 3회, 성공 후 다음 시도까지 최소 1시간으로 제한합니다. 시작 전 DB에 시도를 기록하고 비정상 종료·시간 초과·불명확한 로그인 결과는 outcome_unknown으로 남겨 자동 재시도하지 않습니다. AC07/V07과 연결됩니다.

### T08 독립 브라우저 런타임 배치

T07 이후 별도 Linux 이미지와 명시적 Compose 설정, 자격 증명 비노출 CLI 입력, 제한된 리소스·권한·프로세스 회수 경로를 구현합니다. 계정 파일 미설정은 비활성이고 기존 워커 이미지는 브라우저 의존성을 받지 않습니다. 로컬 브라우저 fixture로 입력·응답·종료·네트워크 경계를 검증합니다. 운영 이미지 빌드와 전송은 기존 서비스 runbook을 따릅니다. AC08/V08과 연결됩니다.

### T09 관리자 복구 상태와 전용 계정 실연결

T07/T08 이후 Iris Admin에 복구 상태·마지막 시도·추가 인증 필요·결과 불명과 복구 방법을 표시합니다. 계정 정보는 보호된 설정 절차로 받고 제공 전에는 임의 계정을 만들거나 현재 인증을 무효화하지 않습니다. 사용자 계정이 준비되면 실제 Linux 로그인·후보 검증·감지 재개를 확인하고 운영 API의 다른 변경을 보존하여 배포합니다. CAPTCHA/2FA의 사용자 개입은 보호된 Linux 브라우저 접속 경로를 검증한 뒤 안내합니다. AC09/V09와 연결됩니다.
## Acceptance

### AC01 비용 없는 발견

계정 ID에서 현재 스페이스를 발견하며 공식 API/유료 공급자 경로가 없습니다. 응답 이상, 인증·제한 오류가 오프라인으로 바뀌지 않습니다.

### AC02 중복 없는 시작 알림

호스트의 방송 구독 방에만 스페이스 링크를 전달합니다. 반복 관측·재시작·제목 변경으로 중복 발송하거나 기존 YouTube 발송에 섞이지 않습니다.

### AC03 안전한 실행과 비활성 기본값

설정 미지정 시 외부 요청/프로세스가 없고 종료 시 helper를 회수합니다. secret 값은 인자·로그·검증 산출물에 없습니다.

### AC04 실계정과 발송 검증

지정 X 계정의 인증으로 실제 감지를 확인하고 지정 방에 발송을 검증합니다. 봇 인증 유지의 확인된 범위와 재인증이 필요한 조건을 명시합니다.

### AC05 관리자에서 재연결

권한 있는 사용자가 Iris Admin에서 새 세션을 제출하고 검증 결과를 조회할 수 있습니다. 실패 시 기존 세션 보존, stale 관측 차단, secret 응답·로그·브라우저 저장 비노출을 검사합니다.

### AC06 기존 알림 컨벤션 준수

스페이스 알림은 전용 DB 템플릿으로 렌더링되며 멤버명과 제목의 Markdown 특수문자를 무력화합니다. 제목 유무와 사용자 정의 템플릿을 지원하고 템플릿 누락은 오류로 보존합니다. 기존 방송 구독과 전송 경로는 바뀌지 않습니다.

### AC07 제한된 자동 복구

새 계정 또는 인증 거부에만 로그인하며 네트워크 오류·요청 제한은 재로그인으로 바꾸지 않습니다. 재시작·경쟁·수동 갱신·후보 거부·시도 상한·불명확한 결과에도 중복 로그인이나 오래된 세션 덮어쓰기가 없습니다. 로그인 성공 주장만으로 활성 인증을 교체하지 않습니다.

### AC08 브라우저와 비밀 격리

로그인 서비스는 브라우저를 필요한 때만 실행하고 종료 시 하위 프로세스를 회수합니다. 비밀번호·쿠키는 인자·환경·로그·녹화·화면 캡처·빌드 산출물에 없습니다. 평상시 알림 워커는 로그인 브라우저와 독립적이며 Windows가 필요하지 않습니다.

### AC09 관리자 관측과 실제 복구

관리자 화면은 로그인 중·후보 검증·추가 인증 필요·중단/결과 불명을 구분합니다. 실제 전용 계정의 Linux 로그인 후 새 쿠키가 검증되고 정기 감지가 재개됩니다. 계정 미제공이나 추가 인증이 실검증을 막으면 미완료로 명시합니다.
## Validation

### V01 수집 모의 검사

`npm test --prefix hololive/hololive-alarm-worker/xspaces`로 발견·호스트·상태·잘못된 응답·인증·제한·redirect 및 비용 경로 차단을 검사합니다.

### V02 발송 회귀 검사

`go test ./hololive/hololive-shared/pkg/domain ./hololive/hololive-shared/pkg/service/alarm/dispatchoutbox ./hololive/hololive-alarm-worker/internal/service/xspaces ./hololive/hololive-alarm-worker/internal/service/dispatchrun`과 필요한 DB migration 재생으로 payload 보존, 중복 키, 렌더링을 검사합니다.

### V03 통합 및 계약 검사

`go test ./hololive/hololive-alarm-worker/internal/app/workerapp ./hololive/hololive-alarm-worker/internal/service/workerruntime`, 관련 architecture/migration/stack gates 및 `git diff --check`를 실행합니다. 검사 결과와 실연결 제한을 같은 문서에 기록합니다.

### V04 실연결 확인

계정 세션 연결 후 제한된 실제 조회와 지정 방 테스트를 실행합니다. 계정 식별·쿠키 원문·메시지 원문 대신 인증 결과와 발송 receipt 등 필요한 비밀 제외 증거만 기록합니다. 단기 성공을 장기 인증 유지의 근거로 삼지 않습니다.

### V05 관리자 계약과 복구 검사

Iris Admin의 생성 OpenAPI 계약을 갱신하고 `bash scripts/verify-all.sh`를 실행합니다. Hololive 세션 저장·승격·실패·경쟁 조건과 암호화 검사를 추가합니다.

### V06 템플릿 회귀 및 운영 반영

샘플 키 집합·전체 seed 렌더링·스페이스 제목 유무와 특수문자·DB 템플릿 사용 및 누락·발송 완료 검사를 실행합니다. 마이그레이션과 스키마 계약을 검사하고 로컬 CI 통과 이미지로 워커만 반영합니다. 운영 템플릿과 워커 상태, X 세션의 후속 조회 성공을 확인합니다.

### V07 복구 원장 및 로그인 흐름 검사

DB 경합·중복 claim·재시작 결과 불명·세대 비교·시도 상한·수동 제출 보존과 브라우저 로그인 fixture의 성공/거절/추가 인증/timeout을 검사합니다. retry·worker 계약과 schema snapshot 검사를 실행합니다.

### V08 런타임 격리 검사

고정 버전 의존성·로컬 이미지 아키텍처·비밀 비노출·프로세스 회수·Compose opt-in을 검증합니다. 새 로그인 서비스만으로 fixture를 실행하고 기존 worker CI를 통과합니다.

### V09 관리자와 운영 연결 검사

공유 OpenAPI와 관리자 생성 코드를 검증하고 Iris Admin 전체 verify-all을 실행합니다. 전용 계정 입력과 Linux 실로그인, 활성 세션 승격, 정기 조회 성공을 비밀 제외 증거로 남깁니다. 실제 CAPTCHA/추가 인증의 미확인 경로를 완료로 처리하지 않습니다.
## Handoff

원본 체크아웃의 관련 없는 변경은 보존합니다. 실계정 세션과 발송 설정을 포함하여 진행하며 사용자 지정 대상이 필요한 단계만 입력을 기다립니다. 필요한 필수 검사가 불가능하면 이유와 남은 작업을 명시합니다.
