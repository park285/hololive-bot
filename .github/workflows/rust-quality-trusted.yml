name: Rust Quality Gates Trusted

on:
  push:
    branches:
      - main
    paths:
      - 'admin-dashboard/backend/**'
  workflow_dispatch:

permissions:
  contents: read

concurrency:
  group: rust-quality-trusted-${{ github.ref }}
  cancel-in-progress: true

env:
  CARGO_TERM_COLOR: always
  FORCE_JAVASCRIPT_ACTIONS_TO_NODE24: true
  RUSTFLAGS: -D warnings

jobs:
  fmt:
    name: Format
    runs-on: [self-hosted, linux, x64, hololive-ci]
    timeout-minutes: 5
    steps:
      - uses: actions/checkout@v6.0.2
      - uses: dtolnay/rust-toolchain@stable
        with:
          components: rustfmt
      - run: cargo fmt --check --manifest-path admin-dashboard/backend/Cargo.toml

  clippy:
    name: Clippy
    runs-on: [self-hosted, linux, x64, hololive-ci]
    timeout-minutes: 15
    steps:
      - uses: actions/checkout@v6.0.2
      - uses: dtolnay/rust-toolchain@stable
        with:
          components: clippy
      - uses: Swatinem/rust-cache@v2
        with:
          workspaces: admin-dashboard/backend
      - run: cargo clippy --manifest-path admin-dashboard/backend/Cargo.toml -- -D warnings

  test:
    name: Test
    runs-on: [self-hosted, linux, x64, hololive-ci]
    timeout-minutes: 15
    steps:
      - uses: actions/checkout@v6.0.2
      - uses: dtolnay/rust-toolchain@stable
      - uses: Swatinem/rust-cache@v2
        with:
          workspaces: admin-dashboard/backend
      - run: cargo test --manifest-path admin-dashboard/backend/Cargo.toml

  deny:
    name: Dependency Audit
    runs-on: [self-hosted, linux, x64, hololive-ci]
    timeout-minutes: 10
    steps:
      - uses: actions/checkout@v6.0.2
      - uses: EmbarkStudios/cargo-deny-action@v2
        with:
          manifest-path: admin-dashboard/backend/Cargo.toml
          command: check
          command-arguments: --config admin-dashboard/backend/deny.toml
