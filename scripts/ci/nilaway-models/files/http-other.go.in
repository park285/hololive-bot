package contract

import "net/http"

type otherClient struct{}

func (*otherClient) Do(*http.Request) (*http.Response, error) {
	return nil, nil
}

func unrelatedDo(req *http.Request) int {
	response, err := new(otherClient).Do(req)
	if err != nil {
		return 0
	}
	return response.StatusCode
}
