#!/usr/bin/env bash
set -euo pipefail

echo "[Runtime] Cross-dependency gate (Go-only runtime)"
echo "[Runtime] Active services: bot, dispatcher-go, llm-scheduler, stream-ingester"
echo "[Runtime] Legacy Rust cross-dependency gate retired"
