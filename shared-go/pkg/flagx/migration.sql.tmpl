-- Migration: Create {{.TableName}} flag table
-- Entity: {{.EntityName}}
-- Generated by: flagx migration template
--
-- Usage:
--   Replace {{.TableName}} with your table name (e.g., user_flags, thread_flags)
--   Replace {{.EntityName}} with your entity name (e.g., User, Thread)
--
-- Example:
--   For user_flags table:
--   - TableName: user_flags
--   - EntityName: User

CREATE TABLE IF NOT EXISTS {{.TableName}} (
    entity_id   TEXT NOT NULL,
    flag        TEXT NOT NULL,
    created_at  TIMESTAMPTZ NOT NULL DEFAULT NOW(),
    created_by  TEXT NOT NULL,  -- trace/correlation ID
    
    PRIMARY KEY (entity_id, flag)
);

-- Index for flag-based queries (find all entities with a specific flag)
CREATE INDEX IF NOT EXISTS idx_{{.TableName}}_flag 
ON {{.TableName}} (flag, entity_id);

-- Index for entity-based queries (covered by PK, but explicit for clarity)
CREATE INDEX IF NOT EXISTS idx_{{.TableName}}_entity 
ON {{.TableName}} (entity_id, flag);

-- Example usage after migration:
-- INSERT INTO user_flags (entity_id, flag, created_by) VALUES ('user123', 'premium', 'trace-abc');
-- SELECT * FROM user_flags WHERE entity_id = 'user123';
-- SELECT * FROM user_flags WHERE flag = 'premium';
