apiVersion: v1
kind: Service
metadata:
    name: valkey-cache
    namespace: hololive
spec:
    selector:
        app: valkey-cache
    ports:
        - name: valkey
          port: 6379
          targetPort: valkey
    type: ClusterIP
