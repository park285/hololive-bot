apiVersion: v1
kind: Service
metadata:
  name: dispatcher-go
  namespace: hololive
spec:
  selector:
    app: dispatcher-go
  ports:
    - name: http
      port: 30020
      targetPort: http
  type: ClusterIP
